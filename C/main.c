#include <stdio.h>
 
// Driver code
int main()
{
    int x, y,result;
 
    x = 136;
    y = 10;
 
    // modulo for floating point values
    result = x % y;
    printf("%d", result);
 
    return 0;
}